/**
 * Placeholder estrutural (Etapa 8) — cases/portfólio real pertence à fase de conteúdo/UI
 * (docs/TECHNICAL-ARCHITECTURE.md, Seção 4).
 */
export default function ProjetosPage() {
  return (
    <main style={{ padding: "2rem", maxWidth: 640, margin: "0 auto" }}>
      <h1>Projetos</h1>
      <p>Página institucional provisória. Cases reais pendentes de fase futura.</p>
    </main>
  );
}
